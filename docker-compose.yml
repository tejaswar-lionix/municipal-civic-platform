version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: civic
      POSTGRES_USER: civic
      POSTGRES_PASSWORD: civic
    ports: ["5432:5432"]
  web:
    build: .
    ports: ["8000:8000"]
    volumes: [".:/app"]
