import React, {useState} from 'react';
export const ResolutionView: React.FC = () => {
  const [filter,setFilter]=useState('high');
  return <div><h2>RESOLUTION - Resolution - workflow, SLA, closure, ver</h2><p>workflow</p></div>
};
export default ResolutionView;
